"use strict"

// Задача -01. Вивести на екран номери місяців весни і літа (від 3 до 8)

if (confirm('Почати тестування?')) {
	for (let i = 3; i <= 8; i++) {
		const result = i <= 5 ? `Цей місяць весняний: ${i}` : `Цей місяць літній: ${i}`

		document.write(`<p>${result}</p>`)
	}
}
