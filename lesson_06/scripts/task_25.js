"use strict"

// Задача 25. Написати програму, що виводить на екран таблицю множення на k (k вводиться з клавіатури).

const k = parseInt(prompt('Введіть будь-яке число', '5'))

for(let i=1;  i<=10; i++) {
    document.write(`<p>${k} * ${i} = ${k*i}</p>`)
}